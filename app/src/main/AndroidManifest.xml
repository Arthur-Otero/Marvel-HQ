<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marvelapidesafio">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MarvelApiDesafio"
        android:usesCleartextTraffic="true">
        <activity android:name=".comics.details.ImageActivity"/>
        <activity android:name=".login.createAccount.CreateAccountActivity" />
        <activity android:name=".comics.ComicActivity" />
        <activity android:name=".comics.details.ComicDetailsActivity" />
        <activity android:name=".login.LoginActivity"/>
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>